## GitLite Docs (Docusaurus)

Local development:

```bash
cd website
npm i
npm run start
```

Build for production:

```bash
npm run build
```

Deploys are handled by GitHub Actions to the `gh-pages` branch.


